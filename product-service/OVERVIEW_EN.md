# Product Service - Recruiter Overview (EN)

`product-service` provides the product catalog and stock source of truth.

## Why it matters

- Uses real persistence with PostgreSQL
- Supports both browsing (`/products`) and stock checks (`/stock/{id}`)
- Powers business decisions made by `order-service`
