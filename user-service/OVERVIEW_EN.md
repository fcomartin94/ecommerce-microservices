# User Service - Recruiter Overview (EN)

`user-service` implements signup/login and returns JWT tokens for authenticated usage.

## Why it matters

- Demonstrates secure authentication flow
- Uses password hashing and token-based sessions
- Shows practical backend security in a microservices context
